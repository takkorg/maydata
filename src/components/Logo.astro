---
import { Image } from 'astro:assets';
import { SITE } from 'astrowind:config';
import logo from '~/assets/images/logo.png';
---

<span class="self-center flex items-center gap-2 ml-2 rtl:ml-0 rtl:mr-2 text-2xl md:text-xl font-bold text-gray-900 whitespace-nowrap dark:text-white">
  <Image src={logo} alt={SITE?.name || 'MayDATA'} width={108} height={108} format="png" quality={100} class="rounded-full w-9 h-9" />
  {SITE?.name}
</span>
